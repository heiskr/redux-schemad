describe('redux-schemad', () => {
  describe('#field', () => {
    it('should create a field', () => {
      expect(false).true
    })
  })

  describe('#embed', () => {
    it('should create an embed', () => {
      expect(false).true
    })
  })

  describe('#embedMany', () => {
    it('should create an embedMany', () => {
      expect(false).true
    })
  })

  describe('#titleize', () => {
    it('should titleize a camelcase string', () => {
      expect(false).true
    })
  })

  describe('#exists', () => {
    it('should test var is not undefined or null', () => {
      expect(false).true
    })
  })

  describe('#omit', () => {
    it('should skip a key in an object', () => {
      expect(false).true
    })
  })

  describe('#createDefaultState', () => {
    it('should create a default store with field', () => {
      expect(false).true
    })

    it('should create a default store with embed', () => {
      expect(false).true
    })

    it('should create a default store with embedMany', () => {
      expect(false).true
    })
  })

  describe('#createAction', () => {
    it('should create an action creator', () => {
      expect(false).true
    })
  })

  describe('#createBatchActions', () => {
    it('should create four action creators per field', () => {
      expect(false).true
    })
  })

  describe('#createEmbedManyActions', () => {
    it('should create eight action creators per field', () => {
      expect(false).true
    })
  })

  describe('#createActions', () => {
    it('should create actions for fields', () => {
      expect(false).true
    })

    it('should create actions for embeds', () => {
      expect(false).true
    })

    it('should create actions for embedMany', () => {
      expect(false).true
    })
  })

  describe('#findErrors', () => {
    it('should find errors for fields', () => {
      expect(false).true
    })

    it('should find errors for embeds', () => {
      expect(false).true
    })

    it('should find errors for embedMany', () => {
      expect(false).true
    })
  })

  describe('#getNewReducerState', () => {
    describe('field', () => {
      it('should set', () => {
        expect(false).true
      })

      it('should update', () => {
        expect(false).true
      })

      it('should reset', () => {
        expect(false).true
      })

      it('should clear', () => {
        expect(false).true
      })
    })

    describe('embed', () => {
      it('should set', () => {
        expect(false).true
      })

      it('should update', () => {
        expect(false).true
      })

      it('should reset', () => {
        expect(false).true
      })

      it('should clear', () => {
        expect(false).true
      })
    })

    describe('embedMany (all)', () => {
      it('should set', () => {
        expect(false).true
      })

      it('should update', () => {
        expect(false).true
      })

      it('should reset', () => {
        expect(false).true
      })

      it('should clear', () => {
        expect(false).true
      })
    })

    describe('embedMany (one)', () => {
      it('should set', () => {
        expect(false).true
      })

      it('should update', () => {
        expect(false).true
      })

      it('should reset', () => {
        expect(false).true
      })

      it('should clear', () => {
        expect(false).true
      })
    })
  })

  describe('#createReducer', () => {
    it('should skip actions without type, name, or verb', () => {
      expect(false).true
    })

    it('should skip actions on non-members', () => {
      expect(false).true
    })

    it('should not update state if errors', () => {
      expect(false).true
    })

    it('should update state if not errors', () => {
      expect(false).true
    })
  })

  describe('#createFromSchema', () => {
    it('should create actions, actionTypes, defaultState, and reducer', () => {
      expect(false).true
    })
  })
})
